// @flow

// State


type State = {};

const initialState: State = {};

// Action

type Action = {}

// Reducer

export default (state: State = initialState, action: Action) => {
  switch (action.type) {
    default:
      return state;
  }
}